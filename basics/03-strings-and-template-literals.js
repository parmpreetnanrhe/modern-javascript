"use strict";
/**********************
 *  Defining string template literals
 */

let name = "John";
let quantity = 3;
let product = "Hat";
let message = `${name} has purchased ${quantity} ${product}`;
console.log(message);
